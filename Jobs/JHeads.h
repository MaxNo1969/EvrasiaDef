#ifndef TestHeadsH
#define TestHeadsH

enum class EUpDown
{
	Up, Down, None
};

enum class ESpeed
{
	Speed1, Speed2, Speed3, Pass, Stop, Cycle, None
};

enum class ERoll
{
	Forw, Back, Stop, None
};

enum class EZamok
{
	Forw, Back, ForwBack, Stop, None
};
enum class E791
{
	Start, Stop, None
};

enum class ESpeed502
{
	Speed, Stop, None
};

#endif
